<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Enter password to download</title>
  </head>
  <body>
    <% if(locals.error){ %>
    <div style="margin: 1rem auto; max-width: 500px; color: red">
      Incorrect Password
    </div>
    <% } %>

    <form
      method="post"
      style="
        display: grid;
        gap: 0.5rem;
        grid-template-columns: auto 1fr;
        max-width: 500px;
        margin: 0 auto;
      "
    >
      <label id="password">Enter password:</label>
      <input type="password" name="password" id="password" required />
      <button style="grid-column: 2 span" type="submit">DOWNLOAD FILE</button>
    </form>
  </body>
</html>
